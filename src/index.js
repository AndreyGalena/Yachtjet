import React from 'react';
import ReactDOM from 'react-dom/client';
// import './zeroing.css?v=1.1';
import './index.css?v=1.1';
import App from './components/App/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
